/**
 * Write a function named arrayToObject:
 *
 * 1. This function takes an array as it's only argument.
 * 2. Using this array, return an object where each pair of even/odd indexed values become the objects key and value.
 *
 * Note:
 *    - The even indexed values are strings
 *    - The odd indexed values can be any data type
 *
 * For Example:
  let testArr1 = [
      "keyOne", 11, "keyTwo", true
    ]

  let obj1 = arrayToObject(testArr1)
  obj1 // { keyOne: 11, keyTwo: true }


  let testArr2 = [
    'one', 5, 'two', 'hello', 'three', []
  ]

  let obj2 = arrayToObject(testArr2)
  obj2 //{ one: 5, two: hello, three: [] }
  */

function arrayToObject() {
  //Write Code Here
}

// do not touch this export
module.exports = arrayToObject;
