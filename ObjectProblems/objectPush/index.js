/**
 * Write a function named objectPush:
 * This should simulate the Array method .push()
 *
 * 1. Take three arguments, the keyName and value we want in our object, and the object itself.
 * 2. It should add the value to the object, with key equal to keyName.
 * 3. It should return how many properties the object has now. (push returns the length of the array)
 * For Example:
 * const testObject = { }
 * const testReturn = objectPush("testKey", "testValue", testObject) // should return 1
 *
 * testObject should now equal //
 * {"testKey":"testValue"}
 *
 */

function objectPush() {
  //write code here
}

// do not touch this export
module.exports = objectPush;
