/**
 * Write a function named objectPop:
 * This should simulate the Array method .pop()
 *
 * 1. It should take two arguments, a key (from the object) and the object itself.
 * 2. The function should remove the key/value from the object
 * 3. It should also return the value from the object as part of the function return.
 *
 * For Example:
 *
 * const a = { soda: "Fanta", flavor: "Grape" }
 * const returnValue = objectPop("flavor", a) // "Grape"
 * console.log(a) // {soda:"Fanta"}
 */

function objectPop() {
  //write code here
}

// do not touch this export
module.exports = objectPop;
